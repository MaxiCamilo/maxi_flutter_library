import 'package:flutter/material.dart';

class FlexibleOnlyRow extends StatelessWidget {
  final Widget child;

  const FlexibleOnlyRow({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
