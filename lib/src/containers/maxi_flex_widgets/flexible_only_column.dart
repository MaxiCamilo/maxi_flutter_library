import 'package:flutter/material.dart';

class FlexibleOnlyColumn extends StatelessWidget {
  final Widget child;

  const FlexibleOnlyColumn({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
